# Middlewares

This directory contains the current `UserModule` Middlewares surface. Module-specific middleware that runs before or around controller execution.

Current files:

- `AuthenticateMiddleware.php`
