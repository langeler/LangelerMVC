@php
$items = is_array($items ?? null) ? $items : [];
$empty = (string) ($empty ?? '');
@endphp
@if($items === [])
    @if($empty !== '')
        <p>{{ $empty }}</p>
    @endif
@else
    <ul class="code-list">
        @foreach($items as $item)
            <li><code>{{ $item }}</code></li>
        @endforeach
    </ul>
@endif
