# Notifications

This directory contains the current `CartModule` Notifications surface. Module-specific notification classes emitted from cart lifecycle events.

Current files:

- `CartMergedNotification.php`
