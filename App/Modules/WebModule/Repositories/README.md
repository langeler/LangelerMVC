# Repositories

This directory contains the current `WebModule` repository surface. Repositories isolate page persistence and admin summary mapping from controllers and services.

Current files:

- `PageRepository.php`
