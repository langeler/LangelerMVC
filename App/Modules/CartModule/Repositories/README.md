# Repositories

This directory contains the current `CartModule` Repositories surface. Persistence adapters that isolate module data access.

Current files:

- `CartItemRepository.php`
- `CartRepository.php`
