# Responses

This directory contains the current `WebModule` response surface. Responses inherit the framework standard response behavior used by HTML and negotiated JSON routes.

Current files:

- `WebResponse.php`
