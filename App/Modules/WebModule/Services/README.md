# Services

This directory contains the current `WebModule` service surface. Services resolve content from configured sources, apply fallback behavior, and return template-ready page payloads.

Current files:

- `PageService.php`
