# CSS Resources

Source stylesheets for framework-owned presentation assets.

- `langelermvc-theme.css` is the canonical Bootstrap-compatible light/dark/system theme source.
- Keep the matching public copy in `Public/assets/css/langelermvc-theme.css` synchronized for plug-and-play deployments.
