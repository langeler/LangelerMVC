# Listeners

This directory contains the current `ShopModule` Listeners surface. Module event listeners that react to catalog lifecycle changes.

Current files:

- `CatalogActivityNotificationListener.php`
