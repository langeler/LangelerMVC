# Routes

This directory contains the current `WebModule` routing surface. Routes register the public homepage, page lookup endpoint, API page lookup endpoint, and fallback page handler.

Current files:

- `web.php`
