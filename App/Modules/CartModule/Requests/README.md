# Requests

This directory is intentionally present to make the planned `CartModule` architecture visible in the repository.
Place concrete typed request objects and request-specific input handling here as the module is implemented.
