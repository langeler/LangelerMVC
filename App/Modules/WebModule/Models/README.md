# Models

This directory contains the current `WebModule` model surface. Models represent database-backed content when the web module is configured to read pages from persistence instead of the in-memory starter fallback.

Current files:

- `Page.php`
