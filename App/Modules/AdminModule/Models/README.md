# Models

This directory is part of the implemented `AdminModule` module contract. Module domain models built on the framework persistence layer.

`AdminModule` does not currently define its own persisted domain entities.

The module is intentionally implemented as an operational surface over existing framework and module models instead of duplicating storage concepts inside the admin layer.
