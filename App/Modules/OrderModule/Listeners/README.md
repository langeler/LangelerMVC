# Listeners

This directory contains the current `OrderModule` listener surface. Listeners connect order lifecycle events to asynchronous notification workflows.

Current files:

- `OrderLifecycleNotificationListener.php`
