@php
$messageText = (string) ($message ?? '');
$tone = (string) ($tone ?? 'info');
@endphp
@if($messageText !== '')
    <div class="message message--{{ $tone }}">
        <strong>{{ $messageText }}</strong>
    </div>
@endif
