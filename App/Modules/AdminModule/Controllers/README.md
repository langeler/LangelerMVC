# Controllers

This directory contains the current `AdminModule` Controllers surface. HTTP entrypoints that orchestrate request, service, presenter, view, and response flow.

Current files:

- `AdminController.php`
