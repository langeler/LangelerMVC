# Presenters

This directory contains the current `WebModule` presenter surface. Presenters shape page records and fallback content into template-safe view data and metadata.

Current files:

- `PagePresenter.php`
