# Controllers

This directory contains the current `WebModule` controller surface. Controllers keep public page and API page entrypoints thin, then delegate page lookup and presentation shaping to the request, service, presenter, view, and response layers.

Current files:

- `HomeController.php`
