# Listeners

This directory contains the current `CartModule` listener surface. Listeners connect cart behavior to framework events without pushing event orchestration into controllers.

Current files:

- `MergeCartOnLoginListener.php`
