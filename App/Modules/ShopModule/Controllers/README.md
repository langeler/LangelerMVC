# Controllers

This directory contains the current `ShopModule` Controllers surface. HTTP entrypoints that orchestrate request, service, presenter, view, and response flow.

Current files:

- `ShopController.php`
