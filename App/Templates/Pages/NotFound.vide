<section class="{{ (string) ($pageClass ?? 'page-not-found') }}">
    @include('PageIntro', [
        'eyebrow' => 'Fallback',
        'headline' => $headline ?? '',
        'summary' => $summary ?? '',
    ])

    <p class="body-copy">{{ (string) ($body ?? '') }}</p>

    <div class="cta-row">
        <a href="{{ (string) ($callToAction['href'] ?? '/') }}">
            {{ (string) ($callToAction['label'] ?? 'Return Home') }}
        </a>
    </div>
</section>
