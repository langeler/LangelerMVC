# Secure Runtime Storage

This directory is reserved for generated runtime keys and other deployment-local security material.

Do not commit generated files from this directory. The installer and runtime services should create required secrets per environment.
