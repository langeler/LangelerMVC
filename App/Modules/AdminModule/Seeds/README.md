# Seeds

This directory is part of the implemented `AdminModule` module contract. Development and test seed classes for the module.

`AdminModule` does not currently require module-owned seed data.

The admin experience is seeded indirectly through the first-party runtime, user/RBAC, catalog, cart, order, queue, notification, and audit surfaces it manages.
