<section class="stack">
    @include('PageIntro', [
        'eyebrow' => 'AdminModule',
        'headline' => $headline ?? 'Operations',
        'summary' => $summary ?? '',
    ])

    @foreach((array) ($operations ?? []) as $section => $payload)
        <article class="section">
            <h2>{{ ucfirst((string) $section) }}</h2>
            <pre class="system-dump">{{ $view->toJson($payload, JSON_PRETTY_PRINT | JSON_UNESCAPED_SLASHES) }}</pre>
        </article>
    @endforeach
</section>
