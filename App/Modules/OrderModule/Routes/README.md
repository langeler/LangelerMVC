# Routes

This directory contains the current `OrderModule` Routes surface. Route definition files registered by the framework router.

Current files:

- `web.php`

Important surfaces:

- public checkout and return routes
- authenticated order history/detail routes
- authenticated payment lifecycle actions
- CSRF-exempt payment webhook callback routes for provider callbacks
