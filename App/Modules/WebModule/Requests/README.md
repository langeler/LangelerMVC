# Requests

This directory contains the current `WebModule` request surface. Requests normalize public page input and keep upload/security defaults aligned with the framework request base classes.

Current files:

- `WebRequest.php`
