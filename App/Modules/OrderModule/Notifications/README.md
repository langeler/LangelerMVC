# Notifications

This directory contains the current `OrderModule` notification surface. Notifications translate order state changes into database and mail channel payloads.

Current files:

- `OrderStatusNotification.php`
